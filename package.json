{
  "name": "echocare",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "senio": "pnpm --filter echo-senio start",
    "family": "pnpm --filter echo-family start",
    "api": "pnpm --filter @echocare/api dev"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "~5.8.3"
  },
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  }
}
